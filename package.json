{
  "name": "pdfreader",
  "version": "1.0.0",
  "description": "PDF Reader Capacitor App",
  "main": "index.js",
  "scripts": {
    "build": "echo '✅ Using existing www directory for Android'",
    "cap": "npx cap",
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "update": "npx cap update",
    "android": "npx cap open android",
    "build:android": "npm run sync && cd android && ./gradlew assembleDebug",
    "ci:build": "npm ci && npm run sync && npm run copy"
  },
  "keywords": [],
  "author": "Arvin",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/share": "^6.2.1"
  }
}
